{
  "$schema": "interface.schema.json",
  "jsonrpc": "2.0",
  "info": {
    "title": "Time Sync API",
    "class": "TimeSync",
    "description": "TimeSync JSON-RPC interface"
  },
  "common": {
    "$ref": "common.json"
  },
  "methods": {
    "TimeSync.1.time": {
      "summary": "Returns the synchronized time",
      "description": "Use this method to retrieve the last synchronized time.",
      "result": {
        "type": "object",
        "properties": {
          "time": {
            "description": "Synchronized time (in ISO8601 format); empty string if the time has never been synchronized",
            "example": "2019-05-07T07:20:26Z",
            "type": "string"
          },
          "source": {
            "description": "The synchronization source e.g. an NTP server",
            "example": "ntp://example.com",
            "type": "string"
          }
        },
        "required": [
          "time"
        ]
      }
    },
    "TimeSync.1.synchronize": {
      "summary": "Synchronizes time",
      "description": "Use this method to synchronize the system time with the currently configured time source.",
      "result": {
        "$ref": "#/common/results/void"
      },
      "errors": [
        {
          "description": "Returned when the method is called while previously triggered synchronization is in progress.",
          "$ref": "#/common/errors/inprogress"
        },
        {
          "description": "Returned when the source configuration is missing or invalid.",
          "$ref": "#/common/errors/incompleteconfig"
        }
      ]
    },
    "TimeSync.1.set": {
      "summary": "Sets the current time from an external source",
      "description": "Use this method to set the current system time independently of the automatic synchronization. If not already active, the framework's *time* subsystem will also become activated after this call.",
      "params": {
        "type": "object",
        "properties": {
          "time": {
            "optional": true,
            "description": "New system time (in ISO8601 format); if this parameter is omitted then the *time* subsystem will still become activated but without setting the time (thereby notifying the framework that the time has been set externally)",
            "example": "2019-05-07T07:20:26Z",
            "type": "string"
          }
        }
      },
      "result": {
        "$ref": "#/common/results/void"
      },
      "errors": [
        {
          "description": "Returned when the requested time was invalid",
          "$ref": "#/common/errors/badrequest"
        }
      ]
    }
  }
}
